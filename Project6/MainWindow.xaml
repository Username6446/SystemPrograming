<Window x:Class="Project6.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Project6"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="{x:Type Button}">
            <Setter Property="Margin" Value="5"></Setter>
            <Setter Property="Padding" Value="5"></Setter>
            <Setter Property="FontWeight" Value="Bold"></Setter>
            <Setter Property="FontSize" Value="20"></Setter>
        </Style>
        <Style TargetType="{x:Type Label}">
            <Setter Property="FontWeight" Value="Bold"></Setter>
            <Setter Property="FontSize" Value="20"></Setter>
        </Style>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Margin" Value="5"></Setter>
            <Setter Property="VerticalContentAlignment" Value="Center"></Setter>
        </Style>
    </Window.Resources>
    <DockPanel LastChildFill="False">
        <Grid Margin="7" DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="4*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label>From:</Label>
            <TextBox Grid.Column="1" Name="fromTextBox"></TextBox>

            <Label Grid.Row="1">To:</Label>
            <TextBox Grid.Column="1" Grid.Row="1" Name="toTextBox"></TextBox>


            <Button Grid.Column="2" Click="Source_Button_Click">Browse</Button>
            <Button Grid.Column="2" Grid.Row="1" Click="Dest_Button_Click">Browse</Button>
            

        </Grid>
        <Button Grid.Row="2" DockPanel.Dock="Top" Click="Copy_Button_Click">Copy Async</Button>
        <ProgressBar Value="15" Height="15" DockPanel.Dock="Bottom" IsIndeterminate="True"></ProgressBar>
    </DockPanel>
</Window>
